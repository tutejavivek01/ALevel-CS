import { Card } from '@/components/Card';

export default function NewPythonProblemPage() {
  return (
    <Card>
      <h3 className="section-title">New Python problem</h3>
      <p>
        Supporter-only problem authoring form lands here in task 21
        (server-side role enforcement per design.md §5, not just hidden
        from the nav).
      </p>
    </Card>
  );
}
